#include <stdio.h>

int vec[] = {5, 7, 1, 9, 2, 4, 8, 3, 6};

main() {
	printf("The first element in the vector is %d.\n", vec[0]);
	printf("The element in the middle of the vector is %d.\n", vec[4]);
	printf("The last element in the vector is %d.\n", vec[8]);
}
